name: Wiki Sync

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'src/**'
      - 'README*.md'
      - '.github/workflows/wiki-sync.yml'
  workflow_dispatch:

jobs:
  sync-wiki:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout main repository
      uses: actions/checkout@v4

    - name: Checkout wiki repository
      uses: actions/checkout@v4
      with:
        repository: ${{ github.repository }}.wiki
        path: wiki

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore tools and dependencies
      run: |
        dotnet tool restore
        dotnet restore

    - name: Build
      run: dotnet build --no-restore -c Release

    - name: Generate API Documentation
      run: |
        # 各アセンブリごとに別フォルダに出力（上書き防止）
        dotnet defaultdocumentation -a src/PosSharp.Abstractions/bin/Release/net10.0/PosSharp.Abstractions.dll --OutputDirectoryPath temp_abs
        dotnet defaultdocumentation -a src/PosSharp.Core/bin/Release/net10.0/PosSharp.Core.dll --OutputDirectoryPath temp_core
        
        # Python を使用して BOM 除去、ヘッダー挿入、および Wiki リンクの調整を行う
        python3 -c '
        import os, glob, re
        header = """<p align="left"><a href="Home"><img src="images/mainlogo.png" width="40" vertical-align="middle"></a> <b>PosSharp v1.2.0 API Reference</b></p>\n\n---\n\n"""
        # temp_abs と temp_core の両方を処理
        for fpath in glob.glob("temp_*/**/*.md", recursive=True):
            if os.path.isdir(fpath): continue
            with open(fpath, "rb") as f:
                content = f.read()
            if content.startswith(b"\xef\xbb\xbf"):
                content = content[3:]
            text = content.decode("utf-8", errors="ignore")
            # 内部リンクから .md 拡張子を除去
            text = re.sub(r"(\]\((?!http)[^)# ]+)\.md", r"\1", text)
            with open(fpath, "w", encoding="utf-8") as f:
                f.write(header + text)
        '

    - name: Sync files to wiki
      run: |
        # Wiki 内の既存の API 関連ファイルを一度削除
        find wiki -maxdepth 1 -name "PosSharp.*.md" -delete
        
        # 生成された全 Markdown を Wiki のルートにフラットにコピー
        find temp_abs temp_core -name "*.md" ! -name "index*.md" ! -name "Home.md" ! -name "_Sidebar.md" -exec cp {} wiki/ \;
        
        # 画像ディレクトリがあればコピー
        if [ -d "docs/images" ]; then
          mkdir -p wiki/images
          cp -r docs/images/* wiki/images/
        fi
        
        cd wiki
        # 変更がある場合のみコミット・プッシュ
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        if ! git diff --cached --quiet; then
          git commit -m "docs: sync from main repository (automated)"
          git push
        else
          echo "No changes to sync."
        fi
